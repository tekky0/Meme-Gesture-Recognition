# Meme-Gesture-Recognition

for this to work you'll have to make a poseData.csv file (or just a regular .csv and change the directory) then create a folder with images you want the model to recognise.
